# CLAUDE.md

本文件是项目的开发指引与上下文，供 Claude Code 在本仓库工作时遵循。

## 1. 项目概述

构建一个"业务影响分析与根因定位监控系统"，面向运维一线，目标是
**1 分钟发现、15 分钟定位、30 分钟解决**故障。

- 完整需求见 `docs/monitoring-system-requirements.md`，它是唯一权威需求来源。
- 现有 `monitoring_report.py` 是监控覆盖率统计工具，属早期产物，可复用其
  数据读取思路，但不是目标系统。

## 2. 开发方法论（卡帕西推荐的方式）

参考 Andrej Karpathy 关于 AI 辅助开发的观点：**生成不是瓶颈，验证才是**。
因此所有开发遵循以下原则。

### 2.1 规格先行（Spec first）

- 动手写代码前，先在需求文档或设计说明里把"要做什么、做成什么样、
  怎么算完成"写清楚。规格不清就先补规格，不靠猜。
- 需求变化时**先改 `docs/monitoring-system-requirements.md`，再改代码**，
  保持规格与实现一致。

### 2.2 先端到端骨架，再迭代（Walking skeleton）

- 不要先把某一层做完美。先打通一条最薄的端到端链路，让系统能跑起来、
  能看到结果，再逐步加厚。
- 允许骨架阶段用硬编码、假数据、最笨的实现（dumb baseline），
  关键是先有一个可运行、可观察、可验证的整体。

### 2.3 小步、可验证（Small, verifiable chunks）

- 每次改动小到**能被完整审查和验证**。拒绝一次性的大改、千行 diff。
- 一个改动 = 一个清晰目的 = 一次提交。改动之间保持仓库可运行。
- 改完立即验证：跑测试、看输出、在界面上操作。验证不过不进入下一步。

### 2.4 AI 上短绳（Keep the AI on a tight leash）

- 给 AI 的任务要**具体、有边界**：明确文件、函数、预期行为，不下泛泛的指令。
- 宁可多轮小任务，不要一轮大任务。每轮产出都要人能看懂、能验证。
- 不确定的设计决策，先问人、先确认，不要自行扩大改动范围。

### 2.5 自主度与风险匹配（Autonomy slider）

- 低风险、可逆的改动（写代码、改测试、本地运行）可放手做。
- 高风险、难逆的操作（删数据、改 CI、推送、连生产、执行处置动作）
  必须先与人确认。本系统的"一键处置"功能本身也遵循同一原则。

### 2.6 让验证更快（Tighten the loop）

- 优先建设让验证变快的东西：自动化测试、可视化界面、样例数据、
  一键启动脚本。验证越快，迭代越快。
- 每个功能都要有"怎么验证它对不对"的明确手段，写在 PR / 提交说明里。

### 2.7 人审每一行（Review everything）

- AI 产出的代码必须经人审查后才合入。不盲目接受。
- 不引入未经理解的依赖、抽象或"为未来准备"的代码。够用即可。

## 3. 增量构建路线（先骨架、后迭代）

总体迭代仍遵循需求文档第 7 节的三期规划，但每一期内部都按"骨架 → 加厚"推进。

### 阶段 0：端到端骨架（最优先）

打通一条最薄链路，证明整体可行：

1. 选 **1 类业务、1 条交易链路、1 个故障场景**（如一次错误发布）。
2. 从 ES 读取该范围的指标/日志（可先用样例数据）。
3. 跑通：异常检测 → 生成 1 条故障事件 → 故障大盘展示 → 详情页能下钻。
4. 用最简实现，硬编码可接受。目标是端到端能跑、能看到、能验证。

完成标志：注入样例故障，大盘上能看到它，并能点进详情看到受影响业务。

### 阶段 1+：按需求三期规划逐项加厚

- 一期：数据接入、CMDB 对齐、分层拓扑、异常检测、告警收敛、故障大盘、Web 端。
- 二期：交易链路建模与 Trace 打标对账、链路定位、逐层下钻、错误日志排查、移动端。
- 三期：预案库、智能推荐、一键处置、自动复盘、大规模性能优化。

每加一项功能，先接入骨架跑通，再完善细节，并补对应验证手段。

## 4. 技术方向（建议，可在阶段 0 复核后调整）

- **后端**：Python（FastAPI），延续仓库现有 Python 技术栈。
- **前端**：React + TypeScript；移动端先做响应式 Web，必要时再上原生。
- **数据源**：指标/日志读 Elasticsearch；不重建采集链路。
- **平台元数据**：拓扑、链路、故障事件、预案等存关系型数据库（如 PostgreSQL）。
- 选型保持简单，能满足当期为准；规模相关优化（预聚合、增量计算）按需引入，
  不提前过度设计。

## 5. 工作约定

- **开发分支**：`claude/monitoring-system-design-uIteM`，在此分支开发、提交、推送。
- **提交**：一个改动一次提交，信息说明"为什么"，保持每次提交后仓库可运行。
- **不擅自**：不创建 PR、不推其他分支、不跳过校验，除非明确要求。
- **文档同步**：需求或设计变化时同步更新 `docs/` 与本文件。

## 6. 关键文件

- `docs/monitoring-system-requirements.md` — 系统需求（权威来源）。
- `CLAUDE.md` — 本文件，开发方法与约定。
- `monitoring_report.py` — 早期监控覆盖率工具。
- `data/` — 样例数据。
